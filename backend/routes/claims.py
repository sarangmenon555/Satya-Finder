# This Script handles the routing of all API calls to thier specific functions
# All calls to the API are routed using this script, the task is executed and the output is returned to the API Call
# This script is not currently built as it requires the creation of the agent loop in order to execute the main model functions
# TODO: Create the Functions that the API Calls are routed to
# NOTE: This is the most important file in the backend